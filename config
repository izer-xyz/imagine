#!/bin/env -i sh

profile=armvirt-32

openwrt_version=22.03.3

packages=\
  -mkf2fs -e2fsprogs -fstools -firewall4 -ca-bundle -dnsmasq \
  -dropbear -kmod-nft-offload -libustream-wolfssl -logd -nftables \
  -odhcpd-ipv6only -opkg -ppp -ppp-mod-pppoe -uclient-fetch

disabled_services=dhcp

env
